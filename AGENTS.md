# CircuitPlatform Agent 约定

## 项目目标

CircuitPlatform 不只是一个能运行的应用，也是一个用于系统学习软件工程的教学项目。任何开发任务都应同时关注功能结果和学习过程。

## 工作要求

1. 开始任务前先说明角色、目标、负责范围和验收标准。
2. 优先保持模块接口小而清晰，把复杂行为封装在模块内部。
3. 修改代码时同步考虑测试、文档和错误处理。
4. 不跨越任务范围修改无关模块。
5. 重要设计选择记录在 `docs/decisions/` 中。
6. 每个阶段结束时更新学习记录和复盘内容。

## 目录职责

- `apps/desktop/`：Electron、Vue 和 TypeScript 桌面应用。
- `packages/protocol/`：前端和 C++ 引擎共享的消息协议类型。
- `engine/`：C++ 数字电路仿真引擎。
- `docs/`：项目、架构、学习和协作文档。

## AI 角色

AI 参与任务时必须遵守 [AI 角色约定](docs/ai/roles.md)。AI 教师负责开发过程中的讲解、拆分任务、提问、评审和复盘，不会成为桌面应用中的运行时功能。
